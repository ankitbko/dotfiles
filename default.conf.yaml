- defaults:
    link:
      create: true
      relink: true
      force: false

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout

- create:
    - ~/downloads
