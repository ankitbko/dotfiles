- defaults:
    link:
      create: true
      relink: true
      force: false

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.gitconfig: gitconfig
    ~/.config/nvim/init.vim: init.vim

- create:
    - ~/downloads
